<h3><span><%= notice %></h3><span>
</br>
<h1><span>Reservations</span></h1>
<div class="tables-center">
    <table class="tables table-striped table-dark">
      <thead>
        <tr class="bg-primary">
          <th>Drop Off</th>
          <th>Pick Up</th>
          <th>Price</th>
          <th colspan="3"></th>
        </tr>
      </thead>
    
      <tbody>
        <% @reservations.each do |reservation| %>
          <tr>
            <td><%= reservation.dropoffdate %> - <%= reservation.dropofftime.strftime("%I:%M%p")    %> </td>
            <td><%= reservation.pickupdate %> - <%= reservation.pickuptime.strftime("%I:%M%p") %> </td>
            <td>€ <%= "%.2f" % reservation.total.truncate(2) %></td>
            <td style="width:5%"><%= link_to 'More Details', reservation_path(:id => reservation.id), :class => 'btn btn-success' %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
</div>    

<div class="bottom_btn">
    <%= link_to 'New Reservation', shops_path, class: "btn btn btn-custom" %>
</div>    